Please make sure you have the proper drivers (framework drivers) for the led matrix firmware. They should appear under "Ports" in the windows device manager. 

I can run this build locally, try the executable in \target\release\led_controller.exe
Thanks so much!
